import type { NextConfig } from 'next'

// Security headers are added in a later Phase 2 step, once every route
// exists and can be verified against the deployed CSP.
const nextConfig: NextConfig = {}

export default nextConfig
