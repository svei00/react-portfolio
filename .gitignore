# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
/build

# testing
/coverage

# production
/build
/.next

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# this repo standardizes on yarn.lock, not npm's lockfile
package-lock.json

# Raw Firebase Firestore export (recovered old portfolio entries, Phase 3
# content-recovery work) — a working reference for pulling real project
# data into src/content/projects.json, not something the app reads at
# build or run time. Keeping the raw export out of the public repo since
# it's scratch data, not a maintained source of truth.
/data
